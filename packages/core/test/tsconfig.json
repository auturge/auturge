{
    "extends": "../conf/tsconfig.base.json",
    "compilerOptions": {
        "moduleResolution": "node", // Tell tsc to look in node_modules for modules
        "outDir": "./out-tsc",
        "types": [
            "jasmine"
        ]
    },
    "include": [
        "../src/**/*.ts",
        "./**/*.ts"
    ]
}
